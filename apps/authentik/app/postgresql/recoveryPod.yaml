kind: ServiceAccount
apiVersion: v1
metadata:
  name: recovery
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: recovery-scc
subjects:
  - kind: ServiceAccount
    name: recovery
    namespace: ${MY_NAMESPACE}
roleRef:
  kind: ClusterRole
  name: system:openshift:scc:anyuid # TBD what is min to mount NFS?
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: v1
kind: Pod
metadata:
  name: recovery-pod
spec:
  containers:
  - image: postgres:latest
    imagePullPolicy: Always
    name: main
    # Just spin & wait forever
    command: [ "/bin/bash", "-c", "--" ]
    args: [ "while true; do sleep 30; done;" ]
    env:
    - name: PGHOST
      valueFrom:
        secretKeyRef:
          key: host
          name: ${MY_NAMESPACE}-db-app
    - name: PGPORT
      valueFrom:
        secretKeyRef:
          key: port
          name: ${MY_NAMESPACE}-db-app
    - name: PGDATABASE
      valueFrom:
        secretKeyRef:
          key: dbname
          name: ${MY_NAMESPACE}-db-app
    - name: PGPASSWORD
      valueFrom:
        secretKeyRef:
          key: password
          name: ${MY_NAMESPACE}-db-app
    - name: PGUSER
      valueFrom:
        secretKeyRef:
          key: username
          name: ${MY_NAMESPACE}-db-app
    resources:
      requests:
        cpu: 5m
        memory: 10Mi
    volumeMounts:
    - mountPath: /backup
      name: backup
  serviceAccount: recovery
  volumes:
  - name: backup
    nfs:
      path: /volume1/kubernetes/backup/db/authentik
      server: nas.home.angelnu.com
